# 🚀 دليل نشر Smart Oasis على Glitch

## ما هو Glitch؟
Glitch منصة استضافة مجانية وسهلة الاستخدام، لكنها:
- ✅ مجانية بالكامل
- ✅ سهلة الاستخدام
- ❌ تغلق المشروع بعد فترة عدم النشاط
- ❌ ليست مناسبة للإنتاج

---

## ⚠️ تحذير مهم:
Glitch مناسب فقط للاختبار والتطوير، ليس للإنتاج. إذا أردت نشر النظام للاستخدام الدائم، استخدم Render أو Sevalla.

---

## الخطوة 1: إنشاء حساب على Glitch

1. افتح [glitch.com](https://glitch.com)
2. اضغط "Sign in"
3. اختر "Sign in with GitHub"
4. اضغط "Authorize glitch"

---

## الخطوة 2: إنشاء مشروع جديد

### 2.1 إنشاء مشروع فارغ
1. بعد تسجيل الدخول، اضغط "New Project"
2. اختر "glitch-hello-node" (أو أي مشروع Node.js)
3. سيتم إنشاء مشروع جديد

---

## الخطوة 3: استيراد المشروع من GitHub

### 3.1 استيراد من GitHub
1. في مشروعك الجديد، اضغط على "Project Info" (أيقونة المعلومات)
2. اختر "Import from GitHub"
3. ابحث عن "Smatr-Oaisi"
4. اضغط "Import"

---

## الخطوة 4: إعداد المشروع

### 4.1 إعداد مجلد backend
بما أن Glitch لا يدعم مجلدات فرعية مباشرة، نحتاج لنقل ملفات backend:

1. اذهب إلى مجلد المشروع
2. انسخ جميع الملفات من مجلد `backend` إلى المجلد الرئيسي:
   - `server.js`
   - `package.json`
   - `package-lock.json`
   - `tickets.db` (إذا موجود)
   - مجلد `uploads` (إذا موجود)

### 4.2 تحديث package.json
تأكد من أن `package.json` يحتوي على:
```json
{
  "name": "smartoasis-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "sqlite3": "^5.1.7",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  }
}
```

---

## الخطوة 5: إضافة متغيرات البيئة

### 5.1 إضافة متغيرات البيئة
1. اضغط على ".env" في القائمة الجانبية
2. أضف المتغيرات التالية:
```
PORT = 3000
NODE_ENV = production
FRONTEND_URL = https://smartoasis.vercel.app
JWT_SECRET = smartoasis_secure_jwt_2024_secret_key
```

---

## الخطوة 6: النشر

### 6.1 تشغيل المشروع
1. اضغط على "Logs" في القائمة الجانبية
2. اضغط على "Show" لعرض السجلات
3. سيقوم Glitch بتشغيل المشروع تلقائياً
4. انتظر حتى يظهر "Server running on port 3000"

---

## الخطوة 7: الحصول على رابط المشروع

### 7.1 الحصول على الرابط
1. اضغط على "Show" في الزاوية اليمنى العليا
2. سيظهر رابط مثل: `https://your-project-name.glitch.me`
3. انسخ هذا الرابط

---

## الخطوة 8: نشر الواجهة الأمامية على Vercel

### 8.1 إنشاء حساب على Vercel
1. افتح [vercel.com](https://vercel.com)
2. اضغط "Sign up"
3. اختر "Sign up with GitHub"

### 8.2 استيراد المشروع
1. اضغط "Add New" → "Project"
2. ابحث عن "Smatr-Oaisi"
3. اضغط "Import"

### 8.3 إعدادات النشر
```
Framework Preset: Vite
Root Directory: frontend
Build Command: npm run build
Output Directory: dist
```

### 8.4 إضافة متغيرات البيئة
```
VITE_API_URL = https://your-project-name.glitch.me
VITE_NODE_ENV = production
```

### 8.5 النشر
1. اضغط "Deploy"
2. انتظر حتى ينتهي النشر
3. احصل على رابط الواجهة الأمامية

---

## الخطوة 9: تحديث متغيرات البيئة

### 9.1 تحديث FRONTEND_URL في Glitch
1. عد إلى مشروع Glitch
2. اضغط على ".env"
3. حدّث `FRONTEND_URL` إلى رابط Vercel الجديد
4. سيتم إعادة تشغيل المشروع تلقائياً

---

## الخطوة 10: اختبار النظام

1. افتح رابط الواجهة الأمامية من Vercel
2. قم باختبار تسجيل الدخول
3. قم بإنشاء تذكرة جديدة
4. تأكد من أن جميع الوظائف تعمل

---

## ✅ الخلاصة

بعد إكمال هذه الخطوات، سيكون لديك:
- **رابط الخادم الخلفي**: `https://your-project-name.glitch.me`
- **رابط الواجهة الأمامية**: `https://smartoasis.vercel.app`

---

## ⚠️ ملاحظات مهمة:

### Glitch:
- المشروع سيغلق بعد فترة عدم النشاط
- عند إعادة فتح المشروع، سيحتاج وقت لإعادة التشغيل
- البيانات قد تفقد إذا تم إغلاق المشروع لفترة طويلة
- ليس مناسباً للإنتاج

### Vercel:
- مناسب للإنتاج
- رابط دائم
- لا يغلق المشروع

---

## 🆛 استكشاف الأخطاء

### مشكلة: المشروع لا يعمل
**الحل**: تحقق من سجلات الأخطاء في قسم "Logs"

### مشكلة: قاعدة البيانات فارغة
**الحل**: Glitch يستخدم قاعدة بيانات مؤقتة، البيانات قد تفقد

### مشكلة: الواجهة الأمامية لا تتصل بالخادم
**الحل**: تأكد من أن `VITE_API_URL` صحيح

---

## 📞 المساعدة

إذا واجهت أي مشاكل، أخبرني بالخطوة التي توقفت فيها!
